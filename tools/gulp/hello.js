module.exports = function (gulp, config, plugins) {
    return function () {
        console.log(plugins.util.colors.blue("// Gulp: Welcome  --------------------------------------------------------------------------------------------->"))
    };
};
